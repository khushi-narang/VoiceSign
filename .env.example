# Copy this file to .env and fill in real values for local development
# Example PostgreSQL URL format (SQLAlchemy):
# DATABASE_URL=postgresql+psycopg2://user:password@hostname:5432/dbname
DATABASE_URL=

# Flask session secret
SESSION_SECRET=change_me

# Optional: path to the videos dataset used by the app
ISL_DATASET_PATH=./data/videos

# Optional: explicit ffmpeg executable path (if not on PATH)
# FFMPEG_PATH=C:\\path\\to\\ffmpeg.exe

# Other options
# DEBUG=true
